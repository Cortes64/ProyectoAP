<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoAP"
        tools:targetApi="31">
        <activity
            android:name=".MainMenuSchoolActivity"
            android:exported="false"/>
        <activity
            android:name="FormOfertaAcademicaActivity"
            android:exported="false"/>
        <activity
            android:name="SignInActivity"
            android:exported="false"/>
        <activity
            android:name="SignInStudentActivity"
            android:exported="false"/>
        <activity
            android:name="SignInProfessorActivity"
            android:exported="false"/>
        <activity
            android:name="SignInSchoolActivity"
            android:exported="false"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>